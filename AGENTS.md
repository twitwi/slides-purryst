
# AGENTS.md — SlidesPurryst

Working rules for AI agents and contributors:

- Do not git commit.
- Analyze with care.
- Reflect on what you propose.
- Keep architecture clean.

---

## Documentation

- **[AUTHORING.md](AUTHORING.md)** — the full authoring guide: quick start,
  configuration (incl. the `#sp-init` payload and the init-param layer
  precedence), every HTML `<sp-*>` component with its Typst counterpart,
  steps / animations / chunklets / bibliographies, presenting (keymap, spApi),
  and the plugin & architecture chapters.
- **[docs/pipeline.md](docs/pipeline.md)** — the internal, server-side
  pipeline (Typst preprocessing and the boot pipeline).
- **README.md** — install / quick-start matrix and release notes.
